services:
  - type: web
    name: carboncredits-backend
    env: python
    buildCommand: "python3 -m pip install fastapi"
    startCommand: "python3 -c \"
from fastapi import FastAPI

app = FastAPI()

@app.get('/')
def root():
    return {'message': 'API is running'}

@app.get('/health')
def health():
    return {'status': 'ok'}

if __name__ == '__main__':
    import uvicorn
    uvicorn.run(app, host='0.0.0.0', port=10000)
\""
    envVars:
      - key: PORT
        value: 10000
